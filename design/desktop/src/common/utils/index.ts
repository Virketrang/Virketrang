import recursivelyTraverseNestedObject from './recursively-traverse-nested-object'
import formatLastname from './format-last-name'
import flattenObject from './flatten-object'
import bindInputsToStore from './bind-inputs-to-store'
import stringToColour from './string-to-colour'

export default abstract class Utils {
    public static recursivelyTraverseNestedObject = recursivelyTraverseNestedObject
    public static formatLastname = formatLastname
    public static flattenObject = flattenObject
    public static bindInputsToStore = bindInputsToStore
    public static stringToColour = stringToColour
}
