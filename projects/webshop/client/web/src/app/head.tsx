import { Fragment, FunctionComponent } from 'react';

const Head: FunctionComponent = () => {
    return (
        <Fragment>
            <title>Liedecke & Noergaard</title>
            <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        </Fragment>
    );
};

export default Head;
