import AppToolbarComponent from './app-toolbar.component.types'
import { toolbar } from './app-toolbar.component.module.sass'

const AppToolbar: AppToolbarComponent = () => {
    return <div className={toolbar}></div>
}

export default AppToolbar
