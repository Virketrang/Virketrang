import FooterComponent from './footer.component.types';
import styles from './footer.component.module.sass';

const Footer: FooterComponent = () => {
    return <footer className={styles.footer}></footer>;
};

export default Footer;
