import { createUseStyles } from 'react-jss';

const useSidebarStyles = createUseStyles({
    sidebar: { height: '100%', width: '10rem', margin: '4rem 4rem 4rem 8rem' },
});

export default useSidebarStyles;
